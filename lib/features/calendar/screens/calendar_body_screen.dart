import 'package:flutter/material.dart';

class CalendarBodyScreen extends StatelessWidget {
  const CalendarBodyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Calendar Body"),
    );
  }
}
