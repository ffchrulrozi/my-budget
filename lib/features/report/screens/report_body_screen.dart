import 'package:flutter/material.dart';

class ReportBodyScreen extends StatelessWidget {
  const ReportBodyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Report Body"),
    );
  }
}
