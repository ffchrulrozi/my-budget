import 'package:flutter/material.dart';

class SettingBodyScreen extends StatelessWidget {
  const SettingBodyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Setting Body"),
    );
  }
}
