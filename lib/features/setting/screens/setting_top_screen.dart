import 'package:flutter/material.dart';

class SettingTopScreen extends StatelessWidget {
  const SettingTopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Setting Top"),
    );
  }
}
