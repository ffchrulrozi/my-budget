import 'package:flutter/material.dart';

class TodayBodyScreen extends StatelessWidget {
  const TodayBodyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Today Body"),
    );
  }
}
